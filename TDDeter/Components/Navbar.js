import { View } from 'react-native';
import LoginButton from './LoginButton';

export default function Navbar() {

    return (
    <View testID='navbar'>
        <LoginButton />
    </View>
    );
}
